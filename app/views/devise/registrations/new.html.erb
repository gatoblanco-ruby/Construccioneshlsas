<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Registrar nueva cuenta</title>
</head>
<body>
  <div class="card">
    <div class="row">
      <div class="col-md-5 order-md-1">
        <%= image_tag("login/registro.jpg", style: "width: 500px;")%>
      </div>  
        <div class="col-lg-6">
          <div class="p-5">
            <div class="text-center">
              <h1 class="text-gray-900 mb-4">Que orgullosos nos sentimos cuando formas parte de nosotros</h1>
            </div>
  
            <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
            <%= render "devise/shared/error_messages", resource: resource %>

            <div class="field form-group">
            <h1><%= f.label :Correo %></h1>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control form-control-user", placeholder:"Ingrese una direccion de correo electronico"%>
            </div>

            <div class="field">
              <h1><%= f.label :Contraseña %></h1>
            <% if @minimum_password_length %>
            <% end %>
            <%= f.password_field :password, autocomplete: "new-password", class:"form-control form-control-user", placeholder:"Minimo 6 caracteres"%>
            </div>

            <div class="field">
            <h1><%= f.label :Confirmar_contraseña %></h1>
            <%= f.password_field :password_confirmation, autocomplete: "new-password",  class:"form-control form-control-user", placeholder:"Las contraseñas deben coincidir" %>
            <br>
            </div>

            <div class="actions">
            <%= f.submit "Registrarme!", class:"btn btn-primary btn-user btn-block" %>
            </div>
            <% end %>

            <%= render "devise/shared/links" %>
    
          </div>
        </div>
    </div>
  </div>
</body>
</html>
