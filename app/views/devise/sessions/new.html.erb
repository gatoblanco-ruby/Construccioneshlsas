<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
</head>
<body>
  
    <div class="card">
      <div class="row">
        <div class="col-md-5 order-md-1">
          <%= image_tag("login/login.jpg", style: "width: 500px;")%>
        </div>  
          <div class="col-lg-6">
            <div class="p-5">
              <div class="text-center">
                <h1 class="text-gray-900 mb-4">Bienvenido!</h1>
              </div>

              <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
              <div class="field form-group"> 
                <h1><%= f.label :Correo %></h1>
                <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control form-control-user", placeholder:"Ingrese su direccion electronica" %>
              </div>

              <div class="field">
                <h1><%= f.label :Constraseña %></h1>
                <%= f.password_field :password, autocomplete: "current-password",class:"form-control form-control-user", placeholder:"Ingrese su contraseña"%>
              </div>

              <% if devise_mapping.rememberable? %>
             
                <div class="field">
                  <%= f.check_box :remember_me %>
                  <%= f.label :remember_me %>
                </div>
              
              <% end %>
              <div class="actions">
                  <%= f.submit "Ingresar", class:"btn btn-primary btn-user btn-block" %>
                </div>
              <% end %>
              
              <%= render "devise/shared/links" %>
              <br>
              <%= link_to "Soy administrador", new_administrator_session_path, class:"btn btn-danger btn-user btn-block"  %>
              
            </div>
          </div>
      </div>
    </div>
 
</body>
</html>